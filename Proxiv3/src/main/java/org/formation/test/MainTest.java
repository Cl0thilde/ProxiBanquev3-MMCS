package org.formation.test;

//import org.formation.service.IService;
//import org.formation.service.ServiceClient;
//import org.projet.domaine.Adresse;
//import org.projet.domaine.Client;
//import org.projet.domaine.Personne;

public class MainTest {

	public static void main(String[] args) {
		// IDao<Client> dao = new DaoClient();
		// IService<Client> serviceClient = new ServiceClient();
		//
		// Adresse adresse = new Adresse("rue", 77000, "ville");
		// Personne personne = new Personne("nom", "prenom");
		// Client c1 = new Client(personne, adresse, "telephone");
		//
		// Client c2 = new Client(personne, adresse, "telephone");
		//
		// Client c3 = new Client(personne, adresse, "telephone");

		// dao.create(c1);
		// dao.create(c2);
		// dao.create(c3);

		// c1.setIdClient(1);
		// c1.setTelephone("0121545646");
		// dao.update(c1);
		//
		// c2.setIdClient(2);
		// dao.delete(c2);

		// System.out.println(dao.readById(1));
		//
		// System.out.println(dao.readAll());

		// serviceClient.create(c1);
//		serviceClient.update(c1);
//
//		System.out.println(serviceClient.readById(1));
//		System.out.println(serviceClient.readAll());
//
//		serviceClient.delete(c1);
//
//		System.out.println(serviceClient.readById(1));

	}

}
